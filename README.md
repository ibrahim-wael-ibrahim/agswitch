# agswitch

Bootstrap scaffold for the Antigravity account switcher.

## Layout

- `cmd/` for CLI entrypoints
- `internal/` for core packages
- `tests/` for fixtures and integration tests
- `scripts/` for helper scripts

## Quick start

```bash
go test ./...
go run . --help
```
